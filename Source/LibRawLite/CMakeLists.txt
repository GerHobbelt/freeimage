# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

# Create a configure-time dependency on cmake.toml to improve IDE support
if(CMKR_ROOT_PROJECT)
	configure_file(cmake.toml cmake.toml COPYONLY)
endif()

# Target: librawlite
set(librawlite_SOURCES
	"internal/dcraw_common.cpp"
	"internal/dcraw_fileio.cpp"
	"internal/demosaic_packs.cpp"
	"src/libraw_c_api.cpp"
	"src/libraw_cxx.cpp"
	"src/libraw_datastream.cpp"
	cmake.toml
)

add_library(librawlite)

target_sources(librawlite PRIVATE ${librawlite_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${librawlite_SOURCES})

target_compile_definitions(librawlite PUBLIC
	LIBRAW_NODLL
)

target_include_directories(librawlite PUBLIC
	"/"
)

target_link_libraries(librawlite PUBLIC
	libjpeg
)

